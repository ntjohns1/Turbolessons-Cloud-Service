server:
  port: 5002
spring:
  application:
    name: admin-service
  config:
    import: configserver:http://www.turbolessons.com:9999/
  cloud:
    config:
      username: ${CONFIG_USERNAME}
      password: ${CONFIG_PASSWORD}

