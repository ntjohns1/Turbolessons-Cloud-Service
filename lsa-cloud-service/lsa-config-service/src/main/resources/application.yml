encrypt:
  key: SoGk99F1INl7Shp2pYQQPNzFfL+vdHhP8x9VlLLlg8Ok
server:
  port: 9999
  ssl:
    enabled: true
    key-store-type: PKCS12
    key-alias: keystore
    key-store: classpath:keystore.p12
    key-store-password: ${KEY_STORE_PASSWORD}
    key-password: ${KEY_PASSWORD}
spring:
  application:
    name: lsa-config-service
  cloud:
    config:
      server:
        bootstrap: false
        git:
          default-label: main
          uri: https://github.com/ntjohns1/lsa-config.git
      discovery:
        enabled: true
management:
  endpoints:
    web:
      exposure:
        include: '*'
eureka:
  client:
    serviceUrl:
      defaultZone: https://www.turbolessons.com:8761/eureka/