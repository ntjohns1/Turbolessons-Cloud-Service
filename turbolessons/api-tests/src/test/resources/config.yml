server:
  port: 8081
base:
  url:
    dev: "http://localhost:3000"
    prod: "https://www.turbolessons.com"

okta:
  client_id: ${OKTA_API_SERVICES_ID}
  client_secret: ${OKTA_API_SERVICES_SECRET}
  org_url: ${OKTA_ORG_URL}
  scope: test_client

